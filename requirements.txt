# ComfyUI GLSL Package Requirements

# Core dependencies
glsl-compiler>=1.0.0
pyvulkan>=0.2.0

# Utility dependencies (minimal)
numpy>=1.21.0

# Development dependencies (optional, for testing)
pytest>=6.0.0